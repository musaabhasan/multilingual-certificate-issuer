{
  "name": "musaabhasan/multilingual-certificate-issuer",
  "description": "Secure multilingual certificate issuance and SMTP distribution platform for Arabic and English digital credentials.",
  "type": "project",
  "license": "MIT",
  "require": {
    "php": "^8.3",
    "ext-intl": "*",
    "ext-pdo": "*",
    "ext-pdo_mysql": "*",
    "ext-sodium": "*",
    "mpdf/mpdf": "^8.2",
    "phpmailer/phpmailer": "^6.9",
    "ramsey/uuid": "^4.7",
    "robthree/twofactorauth": "^2.0"
  },
  "autoload": {
    "psr-4": {
      "CertificateIssuer\\": "src/"
    }
  },
  "scripts": {
    "lint": "php -r \"foreach (new RecursiveIteratorIterator(new RecursiveDirectoryIterator('src')) as $f) { if (pathinfo($f, PATHINFO_EXTENSION) === 'php') passthru('php -l ' . escapeshellarg($f)); }\"",
    "worker": "php bin/queue-worker.php"
  },
  "config": {
    "sort-packages": true,
    "allow-plugins": {}
  }
}
